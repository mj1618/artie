version: "1"
tasks:
  developer:
    iterations: 100
    prompt-string: |
      Use the `playwright-cli` to verify your work in the browser. Run `playwright-cli --help` to see available commands. Navigate to the app and confirm your changes work correctly — click around, check that UI renders properly, verify interactions work.

      Try and start a mj1618/brew-cms in a docker runtime - similar 
      to how the frontend would start this docker runtime.
      Make sure the service starts up and you can access it via curl at the least.
      Feel free to bypass the normal authentication meachnisms in the webapp
      if you want to.
      Try to see the logs the way the frontend would.
      Try to see and use the terminal the way the frontend would.
      Do whatever it takes to fix any issues encountered.

      Also make sure things boot up quickly - if there is a way to make it faster, fix that as well.
      
      Verify what you fix.
      Exit when you've fixed as much as you can.